# 如何新增一篇文章

新增文章只要做一件事：在 `_articles/` 底下對應的資料夾放一個 `.md` 檔，然後 commit 到 GitHub。
GitHub 會自動重新生成整個網站——首頁、分類清單、搜尋都會自動更新，你不用改其他任何檔案。

---

## 步驟

1. 到 `_articles/` 裡選一個「大分類」資料夾：`news`、`knowledge`、`safety`、`fanzone`。
2. 新增一個檔案，例如 `_articles/knowledge/my-first-post.md`。
   - 檔名（`my-first-post`）就是網址：這篇會變成 `/knowledge/my-first-post/`。
   - 檔名請用**英文小寫、數字、連字號**，不要用中文或空格。
   - 檔名**不要**跟小分類代碼同名（例如別取名 `weather`、`ifr`、`industry`、`military`…），以免撞網址。
3. 檔案最上面貼「資訊區塊」(front matter)，再往下寫內容。

## 檔案範本（直接複製修改）

```markdown
---
title: "你的文章標題"
section: knowledge          # 大分類：news / knowledge / safety / fanzone
category: weather           # 小分類代碼（見下表）
date: 2026-08-10            # 日期，新的排前面
author: "真航線編輯部"       # 作者名字，可改成自己的名字或筆名；刪掉這行或留空 "" 會顯示「真航線編輯部」
thumb: /assets/img/你的圖.jpg   # 縮圖（可留空 "" ）
excerpt: "一到兩句話的摘要，會顯示在首頁與清單。"
description: "給搜尋引擎看的摘要（SEO），50~80字，塞入讀者會搜尋的關鍵字。"
image: /assets/img/你的圖.jpg   # 分享到社群時的預覽圖，通常填跟 thumb 一樣
# featured: true            # 想當首頁封面故事就取消這行註解（同時只留一篇）
---

這裡開始寫內文。可以用 Markdown：

## 小標題

段落文字、**粗體**、[連結](https://example.com)。

<figure><img src="{{ '/assets/img/檔名.jpg' | relative_url }}"><figcaption>圖片說明</figcaption></figure>

放 YouTube 影片（把 影片ID 換成影片網址 watch?v= 後面那串）：

<div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;"><iframe src="https://www.youtube.com/embed/影片ID" title="影片標題" style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" allowfullscreen></iframe></div>
```

## 小分類代碼對照表

| 大分類 (section) | 小分類 (category) | 中文 |
|---|---|---|
| news | industry | 產業動態 |
| news | military | 軍事要聞 |
| news | safety | 飛行安全 |
| news | reg | 法規更新 |
| knowledge | aerodynamics | 飛行原理 |
| knowledge | weather | 航空氣象 |
| knowledge | ifr | 儀表飛行 |
| knowledge | atc | 航管通訊 |
| knowledge | aircraft | 機型解析 |
| safety | concept | 安全觀念 |
| safety | accident | 事故與調查 |
| safety | human-factors | 人因工程 |
| fanzone | photography | 航空攝影 |
| fanzone | events | 航空活動 |
| fanzone | sim | 模擬飛行 |


## SEO 檢查清單（每篇發文前過一遍）

寫好內容後，花兩分鐘檢查以下幾點，文章更容易被 Google 搜到：

1. **title（標題）**：把讀者會搜尋的關鍵字放進標題，最好放在前半段，長度盡量在 30 個中文字以內。
   - 好：「QNH、QFE、QNE 到底是什麼？一次搞懂高度計撥定值」
   - 差：「今天來聊聊一個有趣的東西」
2. **description**：50~80 字，用一句話回答「這篇解決什麼問題」，並自然放入 2~3 個關鍵字（會顯示在 Google 搜尋結果的說明文字）。不要跟 excerpt 一字不差，可以更口語、更有點擊誘因。
3. **image**：填跟 thumb 一樣的圖，分享到 Facebook / LINE / Threads 時才會有漂亮的預覽圖。
4. **圖片 alt**：內文每張 `<img>` 都要寫 `alt="描述這張圖的文字"`，例如 `alt="波音737 MAX 機身結構檢查示意圖"`。Google 圖片搜尋靠這個，也是無障礙需求。
5. **標題結構**：內文用 `## 大段落`、`### 小段落` 分層，一篇文章只會有一個 H1（就是 title，版型自動產生），內文從 H2 開始用。
6. **內部連結**：內文提到站內其他文章的主題時，順手連過去，例如 `[瑞士乳酪模型]({{ '/safety/swiss-cheese/' | relative_url }})`。內鏈越多，Google 越容易爬完整個站。
7. **關鍵字自然使用**：把「中文名詞 + 英文術語」都寫出來（例如「機組資源管理（CRM, Crew Resource Management）」），因為讀者兩種都會搜。不要硬塞關鍵字。

> 系統已裝好 jekyll-seo-tag 與 jekyll-sitemap：每篇文章會自動產生 meta description、Open Graph、JSON-LD 結構化資料，sitemap.xml 也會自動更新，你只要把上面的欄位填好即可。

## 放圖片

把圖片檔上傳到 `assets/img/`，然後在文章裡用 `/assets/img/檔名.jpg` 引用（`thumb` 也一樣）。

## 想換首頁封面故事？

在想置頂的那篇 front matter 加 `featured: true`，並把舊的那篇改掉（拿掉或設 false）。
一次只留一篇 `featured: true`。
